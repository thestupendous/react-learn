/*
  map()
  filter()
  reduce()
*/

const names = ["Sheela", "Meeti", "Sonali", "Preet"]

names.map((name) => {
	return name+" 2"
})
console.log(names)


/*
---- use case -------
names.map((name) => {
	return <h1> {name} </h1>
})
*/
